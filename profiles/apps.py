from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """
    AppConfig for the 'profiles' application.

    This class is used by Django to configure and register
    the 'profiles' app within the project.
    """
    name = 'profiles'
